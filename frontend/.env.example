NEXT_PUBLIC_API_URL=http://localhost:3000/api
# Gateway base; used by axios baseURL fallback in dev
NEXT_PUBLIC_GATEWAY_URL=http://localhost:3000
# Realtime endpoint (ws or wss), e.g. ws://localhost:8080/realtime
NEXT_PUBLIC_REALTIME_URL=ws://localhost:8080/realtime
# AUTH mode: "api" uses backend endpoints; "mock" uses local dummy auth
NEXT_PUBLIC_AUTH_MODE=mock
