# This module contains all the secrets.
# It is designed to securely store and manage sensitive information.
# Ensure proper access control and encryption mechanisms are in place
# to prevent unauthorized access to the secrets.

# DATABASE_URL is the connection string for the database.
# Replace the placeholder values with the actual database credentials.
# Format: "postgresql://<username>:<password>@<host>:<port>/<database>?schema=<schema>"
# Example:
# - <username>: The username for the database (e.g., "pediafor").
# - <password>: The password for the database user.
# - <host>: The hostname or IP address of the database server (e.g., "localhost").
# - <port>: The port number on which the database server is running (e.g., "5432").
# - <database>: The name of the database (e.g., "pediafor").
# - <schema>: The schema to use within the database (e.g., "public").
# Ensure the database is running and accessible before starting the application.

DATABASE_URL="postgresql://pediafor:pediafor@localhost:5432/pediafor?schema=public"

# Additional environment variables may be required for the application to run.
# Check the documentation or README file for details on other required configurations.
# Examples of other variables might include:
# - API keys for third-party services.
# - Application-specific settings (e.g., DEBUG mode, logging levels).
# - Paths to external resources or files.
